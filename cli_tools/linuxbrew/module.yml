requires:
  - git
  # - ruby
provides:
  - brew
after:
  - ../../skeletons/zsh
  # - ../../program_lang/ruby
pre-install: |
  [ -d ~/.linuxbrew ] || {
    git clone https://github.com/Linuxbrew/brew.git ~/.linuxbrew
  }
