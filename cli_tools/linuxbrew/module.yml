requires:
  - git
  # - ruby
after:
  - ../../skeletons/zsh
  # - ../../program_lang/ruby
pre-install: |
  [ -d ~/.linuxbrew ] || {
    git clone https://github.com/Linuxbrew/brew.git ~/.linuxbrew
  }
