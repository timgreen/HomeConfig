requires:
  - bash
  - brew
  - curl
  - gpg
afters:
  - ../../skeletons/zsh
provides:
  - yarn
pre-install: |
  which yarn &> /dev/null || {
    curl -o- -L https://yarnpkg.com/install.sh | bash
  }
  yarn config set prefix ~/.node
