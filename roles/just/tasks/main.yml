---
- block:
  - name: Install just
    package:
      name: just
    become: true

  - name: Setup just zsh completion
    command: stow -v -t ~ -d {{ role_path }}/files -S . --no-folding
    register: just_output
    changed_when: '"LINK" in just_output.stderr'

  tags:
  - just
