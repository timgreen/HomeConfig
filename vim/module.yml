requires:
  - vim
  - brew
  - npm # for ternjs
afters:
  - ../skeletons/vim-plug
post-install: |
  # For vim-LanguageTool plugin.
  which languagetool &> /dev/null || brew install languagetool

  vim +PlugInstall +PlugClean! +qall
