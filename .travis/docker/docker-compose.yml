version: '3'
services:
  debian:
    build:
      context: .
      dockerfile: Dockerfile-debian
    volumes:
      - ../../:/source:ro
      - ../test_home:/home/timgreen
      - ../test_hcm_sync.sh:/test_hcm_sync.sh:ro
    command:
      - /test_hcm_sync.sh
